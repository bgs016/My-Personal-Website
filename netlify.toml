# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# HTTP caching: `npm run build` writes `dist/_headers` (Netlify / Cloudflare Pages). Vercel uses `vercel.json`.
